import { Fav } from '../interfaces/favs.interface';

export const FAV_TRACK_ENDPOINT = `${Fav.TRACK}/:id`;
export const FAV_ALBUM_ENDPOINT = `${Fav.ALBUM}/:id`;
export const FAV_ARTIST_ENDPOINT = `${Fav.ARTIST}/:id`;
